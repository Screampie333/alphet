.env
node_modules/
.claude/

# design reference only (Wayback Machine archive dump, not project source)
referensi/
public/api/
data/snapshots-mock.json
data/chain-cache.json

# old Haboob archives - kept locally, not published
backup/

# wrangler's local cache - contains the account id and account name
.wrangler/
