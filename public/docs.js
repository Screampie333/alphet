// docs.js
// The docs page is static markup, so all it needs is the shared shell plus a
// scrollspy driving the table of contents.

HaboobShell.initDrawer();
HaboobShell.initContractCopy();
HaboobShell.initScrollSpy(".toc-item[data-target]");
