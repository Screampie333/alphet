{
  "name": "alphet",
  "version": "0.1.0",
  "description": "Quality gauge for memecoins on Robinhood Chain - splits every launch into Alpha and Beta and shows where the money went",
  "type": "module",
  "main": "src/run.js",
  "scripts": {
    "start": "node src/run.js",
    "once": "node src/run.js --once",
    "mock": "node src/run.js --once --mock",
    "history": "node src/history.js",
    "backfill": "node src/backfill.js",
    "web": "node src/server.js",
    "publish": "node -e \"import('./src/publish.js').then(m=>{const r=m.publish();console.log(r.files+' files, '+r.kb+' KB written to public/api/')})\"",
    "test": "node test/live-parity.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
